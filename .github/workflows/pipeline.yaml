name: Deploy File

on:
  push:
    branches:
      - main

jobs:
  deploy_pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: echo "GitHub Actions works!"